<% content_for :before_app do %>
  <%= render 'components/featured' %>
<% end %>

<% @recipes.each do |date, recipes| %>
  <%=
    render(
      'components/recipe_cards',
      heading: date.strftime("%A, %B #{date.day.ordinalize}"), 
      recipes: recipes,
    )
  %>
<% end %>
